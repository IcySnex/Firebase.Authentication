<Page
    x:Class="Firebase.Authentication.Sample.UWP.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Firebase.Authentication.Sample.UWP.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:re="using:ReCaptcha.Desktop.UWP.UI"
    xmlns:ui="using:Firebase.Authentication.UWP.UI"
    mc:Ignorable="d">
    <!--<Page.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <FirebaseAuthenticationDictionary xmlns="using:Firebase.Authentication.UWP.UI" />

            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>-->

    <StackPanel>
        <ui:FirebaseAuthenticationButton Icon="{x:Bind ui:Icons.ToImageSource(ui:Icons.EmailAndPassword)}" Style="{StaticResource EmailAndPasswordButtonStyle}" />
        <ui:FirebaseAuthenticationButton Style="{StaticResource EmailAndPasswordButtonStyle}" />
        <!--  Why do the bottom icon not load correctly?  -->


        <Button Click="Button_Click" Content="Fuck" />
    </StackPanel>
</Page>
